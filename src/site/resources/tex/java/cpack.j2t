        Model m = new CPModel();
        m.addConstraint(pack(new int[]{5,3,2,6,8,5},5,10, SettingType.ADDITIONAL_RULES.
            getOptionName()));
        Solver s = new CPSolver();
        s.read(m);
        s.solve();
